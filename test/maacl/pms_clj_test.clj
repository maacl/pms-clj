(ns maacl.pms-clj-test
  (:require [clojure.test :refer [deftest is testing]]
            [maacl.pms-clj :as sut])) ; system under test

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
